# AGENTS.md

---

# 项目背景

本项目是 长期维护的项目，不是Demo 或示例程序，任何代码改动必须请求我。

所有修改应优先考虑：

- 可维护性
- 稳定性
- 最小修改原则
- 向后兼容
- 保持现有架构风格

不要为了实现一个新功能而大规模重构已有代码。

---

# 架构目标

实现一个跨平台的播放器。

## 当前架构

```

```

## 阶段目标

最终目标：

- 使用C++ 11-17的语法；
- 本阶段目标是先完成qt的界面，界面要求类似完美解码，中间有一个显示区域，显示区域正下方有个操作区域，包含播放暂停，快进快退，进度条，音量调节等，然后右侧有个播放列表，要求支持同时播放多个文件；
- 先使用qt作为开发的ide，路径在D:\English_Software\qt5.12\Tools\QtCreator\bin，后续再选择支持cmake+vs2022+qt插件的方式，类似obs。
- 先完成qt界面的逻辑，底层播放器的逻辑可以先不处理，注意，底层播放器逻辑我要求生成dll或者so，最外层使用C接口，底层可以使用C/C++，这样我可以提供给java/c#等其他语言时，能快速调用接口；
- 要求跨平台，目前可以先支持windows，且要求支持软解码和硬解码；
- 以后编译，请在项目目录下创建一个编译目录；
- 界面的控件等实现，优先使用纯代码实现，尽量不使用ui，要使用的话，请询问我；
- 函数要加上注释；
---


## 架构修改原则

涉及模块重构（如 OpenGL、EGL、MediaCodec、FFmpeg、DXGI、JNI 等）时：

- 不允许一次性重写整个模块。
- 优先设计迁移方案，再开始编码。
- 每次仅完成一个可编译的小步骤。
- 保持工程始终能够正常编译。
- 优先复用已有代码，不重复实现已有功能。
- 未经确认，不删除旧实现，而是保留作为迁移过渡。
- 对可能影响整体架构的修改，必须先说明设计思路，得到确认后再实施。

## 项目规范

请严格遵守以下规范进行代码修改。

---

## 命名规范

### 类名

类名采用**大驼峰命名法（PascalCase）**。

示例：

```cpp
class CaptureManager
{
};
```

---

### 类成员变量

类成员变量采用**下划线开头 + 全小写 + 下划线**的命名方式。

示例：

```cpp
class CaptureManager
{
private:
    int _frame_count;
    bool _is_running;
    std::string _file_name;
};
```

禁止：

```cpp
int frame_count;
int m_frameCount;
int frameCount;
int FrameCount;
```

---

### 成员函数

成员函数采用**全小写 + 下划线**命名方式。

示例：

```cpp
void start_capture();

int get_frame_count();

bool init_encoder();
```

禁止：

```cpp
startCapture();

StartCapture();

GetFrameCount();
```

---

### 局部变量

局部变量采用**全小写 + 下划线**命名方式。

示例：

```cpp
int frame_count;

std::string file_name;

bool is_running;
```

禁止：

```cpp
frameCount;

fileName;
```

---

### 枚举

枚举类型、枚举值统一采用**全大写 + 下划线**。

示例：

```cpp
enum CAPTURE_STATE
{
    CAPTURE_STATE_IDLE,
    CAPTURE_STATE_RUNNING,
    CAPTURE_STATE_STOPPED
};
```

---


## 注释

### 普通函数和类成员函数注释
/**
* @brief 获取采样格式
* @author: tyy
* @param[in] format 采样格式
* @return 错误码
* @note:
*/


### 类成员变量注释
// 例如
int _a;

---

## 修改代码原则

### 最小修改原则

仅修改完成当前任务所必须修改的代码。

禁止：

- 修改无关代码
- 重构无关模块
- 修改无关命名
- 调整无关格式

---

### 保持项目风格

新增代码必须保持与当前项目一致。

不要为了所谓"最佳实践"而修改已有代码风格。

---

### 优先复用已有代码

新增功能前，请优先查找已有实现。

不要重复实现已有功能。

---

### 不要随意修改命名

未经允许，不要修改：

- 类名
- 成员变量名
- 成员函数名
- 全局变量名
- 文件名

如确有必要，请先征求我的确认。

---

### 不要随意修改接口

除非我明确要求，否则：

- 不修改已有 API
- 不修改函数参数
- 不修改类结构
- 不改变已有行为

---

## 文件操作

### 禁止删除文件

**任何情况下，都不能直接删除项目中的任何文件。**

如果确实需要删除：

**必须先询问我，并得到我的明确确认后才能删除。**

包括但不限于：

- `.cpp`
- `.h`
- `CMakeLists.txt`
- 配置文件
- 图片
- 资源文件
- 测试代码

---

### 禁止重命名文件

未经允许，不要重命名任何文件。

---

### 新建文件

仅在确实必要时创建新文件。

优先在已有文件中完成修改，避免无意义地增加文件数量。

---

## Git 相关

未经允许，不要执行：

- `git reset`
- `git clean`
- `git push --force`
- 删除分支
- 修改 Git 历史

---

## 输出要求

修改代码时：

- 优先输出完整、可编译的代码。
- 不要省略关键实现。
- 不要使用"此处省略""其他代码保持不变"等描述（除非我明确要求）。
- 如果涉及多个文件，请说明修改了哪些文件以及修改原因。

---

## 不确定时

如果你的修改可能：

- 删除文件
- 修改公共接口
- 修改命名
- 影响其他模块
- 改变项目行为

请先询问我，不要自行决定。